═══════════════════════════════════════════════════════════════════════════
                    HOW TO RUN THE APP - SIMPLE VERSION
═══════════════════════════════════════════════════════════════════════════

IMPORTANT FILES ONLY:
  ✓ streamlit_app.py (Web interface)
  ✓ main.py (Command line interface)
  ✓ .env (Your Azure credentials - EDIT THIS)
  ✓ src/ (Application code)
  ✓ prompts/ (Extraction prompts)

EXTRA FILES (Optional - you can delete):
  • check_setup.py (Validation script)
  • test_setup.py (Test suite)
  • run_app.py (Smart launcher)
  • README_SETUP.md (Detailed docs)
  • REFACTORING_SUMMARY.txt (Technical notes)
  • COMPLETION_CHECKLIST.txt (Verification list)
  • START_HERE.txt (Another guide)
  • FINAL_SUMMARY.md (Summary)
  • BEFORE_AND_AFTER.md (Before/after)

═══════════════════════════════════════════════════════════════════════════

STEP-BY-STEP: RUN LOCALLY

═══════════════════════════════════════════════════════════════════════════

STEP 1: Install Dependencies (First Time)
──────────────────────────────────────────

PowerShell:
  cd project-intake-validator
  pip install -r ../requirements.txt

Command Prompt:
  cd project-intake-validator
  pip install -r ../requirements.txt

This installs:
  • pandas - Read Excel files
  • openpyxl - Excel support
  • pydantic - Data validation
  • openai - Azure OpenAI client
  • langgraph - Workflow orchestration
  • streamlit - Web UI
  • python-dotenv - Load .env file


STEP 2: Add Your Azure Credentials
──────────────────────────────────

Edit: .env (in project-intake-validator folder)

Find these lines:
  AZURE_OPENAI_API_KEY=YOUR_NEW_KEY_HERE
  AZURE_OPENAI_ENDPOINT=https://<your-resource-name>.cognitiveservices.azure.com/
  AZURE_OPENAI_DEPLOYMENT_NAME=gpt-4o
  AZURE_OPENAI_API_VERSION=2025-01-01-preview

Replace with your actual values from Azure Portal:

Example .env (filled in):
  AZURE_OPENAI_API_KEY=abc123def456...
  AZURE_OPENAI_ENDPOINT=https://myresource.openai.azure.com/
  AZURE_OPENAI_DEPLOYMENT_NAME=gpt-4o
  AZURE_OPENAI_API_VERSION=2025-01-01-preview

WHERE TO GET THESE:
  1. Go to https://portal.azure.com
  2. Find your OpenAI resource
  3. Click "Keys and endpoint"
  4. Copy API Key (key 1 or 2)
  5. Copy Endpoint URL
  6. Deployment name is the GPT-4 model you created


STEP 3: Run the App
───────────────────

OPTION A: Web Interface (Recommended)
═════════════════════════════════════

PowerShell:
  streamlit run streamlit_app.py

Command Prompt:
  streamlit run streamlit_app.py

Then:
  1. Browser opens automatically
  2. Or go to: http://localhost:8501
  3. Upload an Excel file (.xlsx)
  4. Click "Run Validation 🚀"
  5. View results


OPTION B: Command Line
══════════════════════

PowerShell:
  python main.py path/to/your_file.xlsx

Example:
  python main.py C:\Users\YourName\Documents\intake.xlsx

Output:
  Feedback will print to console


═══════════════════════════════════════════════════════════════════════════

DETAILED EXAMPLE: WEB INTERFACE

═══════════════════════════════════════════════════════════════════════════

1. Open PowerShell or Command Prompt
   
2. Navigate to folder:
   cd C:\Users\YourName\Downloads\project-intake-validator-full-with-reqs\project-intake-validator

3. Run Streamlit:
   streamlit run streamlit_app.py

4. Wait for output:
   "You can now view your Streamlit app in your browser."
   "Local URL: http://localhost:8501"

5. Browser opens automatically to http://localhost:8501

6. You see a form:
   - Upload project Excel file
   - Click "Run Validation 🚀" button
   
7. Application processes:
   - Reads Excel file
   - Sends text to Azure OpenAI
   - Extracts sections (Header, Business Case, Scope)
   - Validates required fields
   - Returns feedback

8. Results displayed:
   - Final Feedback (what's missing)
   - Extracted Sections (JSON)
   - Validation Details


═══════════════════════════════════════════════════════════════════════════

EXCEL FILE FORMAT

═══════════════════════════════════════════════════════════════════════════

Your Excel file should contain:
  
  Header Section:
    - Project Name (required)
    - Project ID
    
  Business Case Section:
    - Problem Statement (required)
    - Expected Benefits (required)
    - Key Metric (required)
    
  Project Scope Section:
    - In Scope (required)
    - Out of Scope (required)

The app automatically extracts these sections from your Excel file.


═══════════════════════════════════════════════════════════════════════════

ERRORS & FIXES

═══════════════════════════════════════════════════════════════════════════

Error: "ModuleNotFoundError: No module named 'openai'"
Fix:    pip install -r ../requirements.txt

Error: "AZURE_OPENAI_API_KEY is not set"
Fix:    Edit .env file with your real credentials (not placeholder text)

Error: "Connection error to Azure"
Fix:    Check API key is correct
        Check endpoint URL is correct
        Check deployment name matches your model

Error: "File does not exist"
Fix:    Verify Excel file path
        Use absolute path: C:\full\path\to\file.xlsx


═══════════════════════════════════════════════════════════════════════════

MINIMAL FILES TO KEEP

═══════════════════════════════════════════════════════════════════════════

If you want to delete extra files, keep ONLY these:

project-intake-validator/
├── streamlit_app.py          ← KEEP
├── main.py                   ← KEEP
├── .env                      ← KEEP (configure with your credentials)
├── QUICK_START.md            ← KEEP (this quick reference)
├── prompts/
│   └── section_extractor.md  ← KEEP
├── src/
│   └── piv/                  ← KEEP (all code)
└── tests/                    ← KEEP (optional)

SAFE TO DELETE (optional docs/utilities):
├── check_setup.py
├── test_setup.py
├── run_app.py
├── README_SETUP.md
├── REFACTORING_SUMMARY.txt
├── COMPLETION_CHECKLIST.txt
├── START_HERE.txt
├── FINAL_SUMMARY.md
├── BEFORE_AND_AFTER.md


═══════════════════════════════════════════════════════════════════════════

COMPLETE EXAMPLE: START TO FINISH

═══════════════════════════════════════════════════════════════════════════

1. Open PowerShell

2. Install (first time only):
   cd project-intake-validator
   pip install -r ../requirements.txt
   
3. Configure .env:
   Edit .env with your Azure credentials
   
4. Run:
   streamlit run streamlit_app.py
   
5. Wait:
   Browser opens to http://localhost:8501
   
6. Upload:
   Click upload button
   Select your Excel file
   
7. Validate:
   Click "Run Validation 🚀"
   
8. View Results:
   See feedback on what's valid/invalid


═══════════════════════════════════════════════════════════════════════════

THAT'S IT!

The app is ready to use. Just follow the 2 main steps:
  1. Install dependencies (one time)
  2. Configure .env with credentials
  3. Run streamlit run streamlit_app.py

═══════════════════════════════════════════════════════════════════════════
